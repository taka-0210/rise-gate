# Server-specific environment settings belong only on the server.
# Configure RISEGATE_ADMIN_PASSWORD in the hosting control panel or in a
# server-owned .htaccess file that is excluded from deployment.
